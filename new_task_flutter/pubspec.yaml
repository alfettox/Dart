name: task_flutter
description: A new Flutter project
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.2
  flutter_lints: ^2.0.0

assets:
  - assets/.env

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
